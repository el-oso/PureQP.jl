bench/pureblas/
docs/build/
docs/node_modules/
Manifest.toml
scratch/
.claude-reap.sh
.claude/
# Per-solve samples the spike scripts regenerate; their compact summaries are tracked.
*/bench/results/*_raw.json
PureIPM/bench/results/ipm_matrixfree_spike.json

# Coverage counters, written beside the source by an instrumented run.
*.cov

# Per-machine StrictMode tier settings; bench/ keeps a tracked one it needs.
LocalPreferences.toml
